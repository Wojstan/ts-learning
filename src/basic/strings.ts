// takes a string as input and returns the length of the string
export function getStringLength(text: string) {
  return 0
}

// reverses a given string
export function reverseString() {}

// capitalizes the first letter of each word in a given sentence
export function stringCapitalization() {}

// compresses a string by counting consecutive characters and replacing them with the character and its count
export function compressString() {}

// checks if one string is a rotation of another string. For example, "waterbottle" is a rotation of "erbottlewat."
export function isRotation() {}

// checks if two strings are anagrams of each other (contain the same characters in different orders)
export function areAnagrams() {}

// finds the length of the longest substring without repeating characters in a given string
export function longestSubstringWithoutRepeating() {}

// tokenizes a sentence into an array of words
export function tokenizeString() {}

// replaces all occurrences of a specified word in a sentence with another word
export function replaceWord() {}

// extracts a substring from a given string based on start and end indices
export function getSubstring() {}

// slices a string from a given start index (inclusive) to an end index (exclusive)
export function getSlice() {}

// splits a sentence into an array of words using a specified delimiter
export function splitString() {}

// returns the first word of a sentence
export function getFirstWord() {}

// returns the last word of a sentence
export function getLastWord() {}

// replaces a substring with another string in a given sentence
export function replaceSubstring() {}

// splits a sentence into words, reverses the order of the words, and then joins them back into a sentence
export function reverseWords() {}

// counts the number of times a substring appears in a given string
export function countSubstringOccurrences() {}

// removes all occurrences of a specified substring from a given string
export function removeSubstring() {}

// finds the first occurrence of a specified substring in a given string and returns its index. If the substring is not found, return -1
export function findSubstring() {}
